"""Bench helper scripts.

Each script in this directory is independently invokable via
``python -m bench.scripts.<name>``. The scripts produce assets the
harness consumes (synthetic corpora, query/answer pairs, etc.) and
are deterministic — same inputs always yield byte-identical outputs.
"""
