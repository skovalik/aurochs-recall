"""Bundled benchmark datasets.

Datasets are JSONL files committed to the repo. They are generated
deterministically by ``bench.scripts.generate_synthetic`` so any
contributor can regenerate the bytes from the seed and verify nothing
hand-edited has crept into the test corpus.
"""
