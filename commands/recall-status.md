---
description: Show recall DB stats — drawer count, WAL size, schema version, last indexed
argument-hint: [--json]
---

The user invoked `/recall-status` with these arguments: $ARGUMENTS

Run `recall status` via the Bash tool and show the user the output. If the user passed `--json` as an argument, run `recall status --json` instead.

```bash
recall status $ARGUMENTS
```
